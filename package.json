{
  "name": "lacona-phrase-validator",
  "version": "0.5.2",
  "description": "Lacona phrase that validates input programmatically, but does not suggest",
  "main": "lib/validator.js",
  "directories": {
    "test": "test"
  },
  "devDependencies": {
    "chai": "^1.10.0",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.5",
    "jshint": "^2.6.0",
    "lacona-util-fulltext": "^0.2.0",
    "lacona-util-jshint": "^0.2.0",
    "mocha": "^2.1.0"
  },
  "scripts": {
    "test": "mocha test",
    "lint": "npm run lint:lib && npm run lint:test",
    "lint:lib": "jshint --config node_modules/lacona-util-jshint/lib.json lib",
    "lint:test": "jshint --config node_modules/lacona-util-jshint/test.json test",
    "validate": "npm run lint && npm run test",
    "cover": "istanbul cover _mocha test -- -R dot && istanbul report text",
    "coveralls": "istanbul cover _mocha test --report lcovonly -- -R dot && cat coverage/lcov.info | coveralls && rimraf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/brandonhorst/lacona-phrase-validator.git"
  },
  "keywords": [
    "lacona",
    "phrase",
    "validate",
    "validator"
  ],
  "author": "@brandonhorst",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/brandonhorst/lacona-phrase-validator/issues"
  },
  "homepage": "https://github.com/brandonhorst/lacona-phrase-validator",
  "dependencies": {
    "lacona": "^0.16.2",
    "lacona-util-phrasehelpers": "^0.1.0"
  }
}
